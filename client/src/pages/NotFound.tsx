import { ArrowLeft, Search } from "lucide-react";
import { useLocation } from "wouter";
export default function NotFound() { const [, navigate] = useLocation(); return <div className="grid min-h-screen place-items-center bg-[#f6f8f4] px-5 text-center"><div><div className="mx-auto mb-8 grid h-14 w-14 place-items-center rounded-[15px] bg-[#141714] font-display text-[20px] font-bold text-[#c8ff4a]">H</div><div className="mb-3 text-[11px] font-bold uppercase tracking-[.2em] text-[#8c998d]">Erro 404</div><h1 className="font-display text-5xl font-semibold tracking-[-.07em] sm:text-7xl">Página não encontrada<span className="text-[#9cbe45]">.</span></h1><p className="mx-auto mt-5 max-w-[350px] text-[13px] leading-6 text-[#7d897e]">Parece que este endereço mudou ou nunca existiu. Vamos levá-lo de volta para um lugar útil.</p><div className="mt-8 flex justify-center gap-2"><button onClick={() => navigate("/")} className="inline-flex items-center gap-2 rounded-full bg-[#141714] px-5 py-3 text-[12px] font-semibold text-white"><ArrowLeft size={14} />Ir para o início</button><button onClick={() => navigate("/app")} className="inline-flex items-center gap-2 rounded-full border border-[#dce5d9] bg-white px-5 py-3 text-[12px] font-semibold text-[#465247]"><Search size={14} />Abrir painel</button></div></div></div>; }
